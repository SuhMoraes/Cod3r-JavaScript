const [a, b, c, d] = [3, 1, 15, 30];

const soma = a + b + c + d;
const subtracao = b - d;
const multiplicacao = a * c;
const divisao = d / a;
const modulo = a % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo);